.DS_Store
**/.DS_Store
.claude/

# ── Compiler intermediate output ──────────────────────────────────────────────
# Object files
*.o
# GCC dependency tracking
*.d
# GCC stack-usage analysis
*.su
# GCC cyclomatic-complexity analysis
*.cyclo
# Build file lists generated by STM32CubeIDE
objects.list

# ── Compiled firmware images ──────────────────────────────────────────────────
# .hex, .elf, and .bin files are intentionally kept so others can flash
# or debug without recompiling.

# ── BLE firmware build output directory ───────────────────────────────────────
# The entire Output/ tree is generated by the SES/SEGGER Embedded Studio build.
FW-BLE/carss_board/s112/ses/Output/

# Claude Code settings
.claude/
**/.claude/

# ── Compiler intermediate output ──────────────────────────────────────────────
# Object files
*.o
# GCC dependency tracking
*.d
# GCC stack-usage analysis
*.su
# GCC cyclomatic-complexity analysis
*.cyclo
# Build file lists generated by STM32CubeIDE
objects.list

# ── Compiled firmware images ──────────────────────────────────────────────────
# .hex, .elf, and .bin files are intentionally kept so others can flash
# or debug without recompiling.

# ── BLE firmware build output directory ───────────────────────────────────────
# The entire Output/ tree is generated by the SES/SEGGER Embedded Studio build.
FW-BLE/carss_board/s112/ses/Output/
